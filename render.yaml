services:
  - type: web
    name: job-scraper
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GITHUB_PAT
        sync: false  # or true if you want to auto-sync from Render dashboard
